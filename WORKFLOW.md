# 工作流走查

一遍完整的使用歷程，用一個具體功能走過七個階段：**「結帳時套用優惠券」**。

---

## 一、七個階段

| 階段 | 吃什麼 | 吐什麼 | 視窗 |
|---|---|---|---|
| `/shan-setup` | 專案本身 | `.shan/config.md` | 任何視窗，一個 repo 跑一次 |
| `/shan-grill` | 你的想法 | `<草稿區>/<slug>/grill.md` | 視窗 A |
| `/shan-to-spec` | `grill.md` | `<草稿區>/<slug>/spec-draft/` | 通常接續 A |
| `/shan-spec-qa` | spec 草稿 | 修訂後的 spec + 品保報告 | 建議新視窗 |
| **你手動把 spec 搬進正式目錄** | | | 人工核可閘門 |
| `/shan-plan` | spec | `session-map.md` + 開場 prompt | 新視窗 |
| `/shan-implement` | 一棒 | 綠燈 + commit + 自動審查 | **每棒一個全新視窗** |
| `/shan-code-review` | commit 後的 diff | 雙軸報告 + `review-S<X>.md` | **必須乾淨 context** |

**視窗紀律只有三處是硬的**：實作每棒各自新視窗、手動審查必須乾淨 context、`shan-spec-qa` 的語意審查必須獨立（它會自己 spawn 冷 subagent，所以在哪跑都行，另開更保險）。

其餘都可以接續同一個視窗。`shan-to-spec` 讀的是 `grill.md` 檔案而不是對話，所以理論上可以新開；接續同一個視窗只是省事，順便保留對話裡沒寫進檔案的細微脈絡。

---

## 二、走一遍：結帳套用優惠券

### 階段 0｜`/shan-setup`

只跑一次。它探索專案、把發現攤開、逐節問你，最後寫出 `.shan/config.md`——那份檔案是後面每一支 skill 的專案知識來源。

### 階段 1｜`/shan-grill`

```
/shan-grill 我想讓使用者結帳時可以套用優惠券
```

它先讀 config，再依 config B 節的順序讀專案文件，然後開場：

> slug 用 `checkout-coupon`。這次審訊要問到「優惠券的作用單位、疊加規則、折扣在整條金額鏈的位置」都定案為止。

**Round 1** —— 前沿是「前置條件都已定案」的那一圈問題：

```
❓ Q1 — 優惠券的作用單位
整張訂單打折，還是單一品項？兩者在退款與部分取消時的攤提方式完全不同。
➡️ 建議：整張訂單。品項級折扣會逼你在退款時處理攤提，那是另一個量級的複雜度。

❓ Q2 — 可以疊加多張嗎
➡️ 建議：不可疊加，一張訂單一張券。疊加會帶出順序、上限、互斥規則三組新問題。

❓ Q3 — 折扣算在稅前還是稅後
➡️ 建議：稅前。稅金應該對實付金額課徵。
```

注意**「疊加時的計算順序」沒有出現在第一輪**——它依賴 Q2。Q2 若答「可疊加」，它才會在下一輪浮上來。這就是決策前沿。

同時它會**自己去查**（不會拿來問你）：現在的金額計算走哪條路徑、有沒有既有的折扣或促銷概念、資料表長怎樣。查核路徑來自 config C 節。

**Round 2** —— Q1–Q3 定案後解鎖的問題：

```
❓ Q4 — 折扣計算放在哪一層
(a) 在 CheckoutService 結帳流程裡直接算（最小 patch）
(b) 做成定價規則的一等公民，併進既有的金額計算鏈（架構一致的正解）
➡️ 建議 (b)。(a) 確實比較快，但金額計算鏈已經是「訂單最終金額」的單一收斂點，
   另開一條平行的折扣邏輯會讓金額來源分裂成兩處。而且下一個促銷需求
   （會員價、滿額折抵）一定會再遇到同一個決定——現在分裂，債會複利。
   成本：要動既有的計算鏈，S2 那一棒會變重。

❓ Q5 — 優惠券失效的判定時點
下單當下，還是付款完成當下？中間若跨過到期時間怎麼算？

❓ Q6 — 折扣要不要留稽核軌跡
```

Q4 就是「兩案併陳 + 推薦立場」在運作——**它不會默默端出 (a) 然後把 (b) 埋進「未來可以考慮」**。

**Round 3** 收斂，前沿清空。產出：

```
<草稿區>/checkout-coupon/grill.md
  ## 已敲定決策   D1–D6，每條含「問題 / 決定 / 理由 / 依據事實（附檔案路徑）」
  ## 待決         （空）
  ## 明確排除     疊加、品項級折扣、優惠券自動推薦
```

### 階段 2｜`/shan-to-spec`

先檢查 `grill.md` 的「待決」是不是空的——不是就停下來叫你回去 grill。**它不會自己替未決事項做決定。**

然後是整支唯一會停下來的地方：

> **議定 seam**
> ① 金額計算鏈的入口（既有 seam，最高點——一個測試能同時涵蓋折扣與稅金的互動）
> ② 結帳 endpoint（既有 seam）
> 不新增 seam。
> 依 config E 節：① 碰 DB → 整合測試基底；② → controller 切片測試。
> prior art：既有的金額計算測試。
>
> 這樣對嗎？

確認後它一次寫完，交付時列出：產出規模（8 條需求、23 條驗收條件、6 個決策、11 個任務）、seam 摘要，以及**它自行補的假設**——那一段要你複核，因為那是 `grill.md` 沒講、它依 codebase 推的。

決策編號 **D1–D6 直接接續 `grill.md`**，不重編。

### 階段 3｜`/shan-spec-qa`

先分級：動到熱路徑（金額計算）✓、含 migration ✓、改動既有流程 ✓ → 三項全中，但需求 8 條未過 10 → 閘門 4 跑一輪。

```
閘門 1 格式    對照專案的格式契約，標題逐字比對                    ✅
閘門 2 識別字  文件裡 backtick 包的識別字逐一實際查證
              自稱既有卻找不到的 = hallucination 的形狀            ✅
閘門 3 機械    任務回指集合 A vs 需求編號集合 B
              B − A = {5.2} ← Req 5.2 沒被任何任務涵蓋            ⚠️
閘門 4 語意    spawn 冷 subagent，只給 spec + codebase
```

閘門 4 的典型收穫，正是那三類反覆出現的問題之一——**新舊路徑交界處**：

> 🔴 spec 只描述了「結帳時套用折扣」，但**退款**也會算訂單金額。退款要按折扣後金額退，還是按原價退？spec 完全沒提，而 `RefundService` 目前直接讀訂單原始金額。

這種東西作者自己重讀十遍也看不到——他會沿著原本的思路再走一次，而錯誤正藏在那條思路沒照到的地方。

修訂之後**重跑閘門 2、3**（修訂本身會製造新的引用漂移）。

### 階段 4｜你手動搬 spec

`spec-draft/` 的檔案由**你**搬進正式的 spec 目錄。skill 不會代做——那一步是人工核可閘門。

### 階段 5｜`/shan-plan`（新視窗）

四維評估（層級 / 依賴 / 複雜度 / 風險）後切三棒：

```
S1  migration + Coupon 資料模型 + 資料存取     不可逆變更 → 高風險 → 獨立一棒
S2  折扣併入金額計算鏈 + 測試                  依賴 S1
S3  結帳 API 帶入優惠券 + 稽核                 依賴 S1；與 S2 無依賴 → 同屬前沿，可換順序
```

然後問**三個具體問題**，不是「這樣 OK 嗎」：

> 1. 粒度對嗎？有哪一棒太粗（一個視窗做不完）或太細（開視窗的成本大於內容）？
> 2. 阻塞關係對嗎？有沒有我排成了序列、其實可以換順序的？
> 3. 有沒有該合併或再拆的？特別是我因為高風險而獨立出來的那幾棒。

產出 `session-map.md` 與可複製的開場 prompt。

### 階段 6｜`/shan-implement`（每棒全新視窗）

貼開場 prompt，它會：

```
讀 config + spec + session-map 的 S1 段
 → 跟你確認任務範圍
 → seam 已在設計文件議定 → 照用，不重新協商
 → 開分支（到這一步才開；前面都是唯讀，隨時可乾淨中止）
 → 紅綠迴圈：一個測試 → 一份最小實作 → 重複
    ⚠️ 明文禁止「先寫完所有測試再寫實作」
 → 跑 config D 節的完整驗證（外部服務不可用就停下來問，不繞過）
 → 勾選任務
 → 依 config G 節處理 commit
 → 自動 spawn 冷 subagent 做第一輪審查
 → 報告 S1 結束，不會自己接著做 S2
```

### 階段 7｜`/shan-code-review`

自動那輪抓明顯的東西；需要判斷力的東西另開乾淨視窗跑。回報**並排、不跨軸重排**：

```
## 意圖軸
Req 3.1 ✅   Req 3.2 🔴 「券在下單與付款之間過期」的邊界沒有測試（spec 原文：…）

## 規範軸
🔴 無
🟡［架構債型 — 附推薦立場］
   折扣金額只存在計算結果裡，沒有任何 INFO log。金額對不上時無從追查是哪張券。
   推薦補一筆里程碑 —— 這個模式會被 S3 的結帳端複製。
🟡［裝飾性］…
```

**兩軸分開報，不挑總冠軍**——完全符合規範但做錯東西、完全照 spec 做但違反慣例，這兩種要能各自被看見。

---

## 三、跑完留下什麼

```
<spec 目錄>/checkout-coupon/     ← 你手動搬進來的，進版控
   需求 / 設計 / 任務三份文件

<草稿區>/checkout-coupon/         ← gitignore，過程產物
   grill.md          決策的來源，含當時查到的事實
   spec-draft/       草稿（已搬走，留著當歷史）
   session-map.md    開發計畫
   review-S1.md      審查記錄（見下一節）

git log    3 個實作 commit + 若干修正 commit，各帶回指 footer
測試        帶驗收條件回指前綴，可對回需求
```

**可追溯性從頭貫到尾**：

```
grill.md D4 → 設計文件 D4 → Req 4.1 → 測試的 Req 4.1 前綴 → commit footer
```

任何一環都能往回查到「當初為什麼這樣決定、依據什麼事實」。

---

## 四、審查回饋怎麼接

### 在哪裡改

**在審查視窗改，不要貼回實作視窗。** 三個理由：

1. **轉述必失真。** 報告在兩個視窗之間搬運，一定會掉東西。
2. **貼回去會觸發辯護。** 實作者有它的理由，你會拿到「我當初這樣寫是因為……」。你要的是修正，不是辯論——這是「作者替實作合理化」的同一種病。
3. 審查者當下就載入了 spec、diff、findings，它是最清楚該改什麼的那個。

**兩個例外要回實作視窗**：你**不同意**審查者的判斷時（貼回去問「你為什麼這樣寫」，目的是取得裁決依據，不是叫它改）；修正牽涉**設計決策**時（那應該上升到你，甚至回 spec）。

### 多輪怎麼接

**每一輪都開全新視窗**，但帶一份累積的 `review-S<X>.md`：

```markdown
## 第 1 輪 — YYYY-MM-DD — 範圍 abc1234...def5678
### 已修正（修正 commit: 9a8b7c6）
- 🔴 Req 3.2 邊界未測試 → 補了 <測試名>
### 已裁決不修（你決定的）
- 🟡 三處按鈕區塊重複 — 理由：S3 會一起重構，現在抽反而要改兩次
### 有異議
- 🔴 審查說會失敗，實際查證是靜默略過 — 結論相同但失效路徑不同，已更正
```

第二輪的開場 prompt 帶上它。這樣同時解掉三件事：**視窗仍然乾淨**、**不重複報你已裁決的東西**、**上一輪的修正本身進入審查範圍**——那段程式碼在此之前沒有任何人審過。

### 怎麼避免越審越肥

多輪審查有個真實的失效模式：**審查者有「找到東西」的壓力**，回報「無發現」看起來像沒盡力，於是每一輪降低門檻——第一輪報結構性問題，第二輪報命名，第三輪報「這裡可以抽個 helper」。而 🟡 建議天然偏向「增加」，累積三輪就是三層沒人要求的抽象。

三個煞車寫在 `shan-code-review` 裡：

- **後續輪次只報 🔴。** 裝飾性建議在第一輪講完；第二輪還在報命名就是門檻滑落。
- **後續輪次縮小範圍**——審修正 commit 的 diff，不是整棒重掃。
- **硬上限三輪。** 第三輪還在出 🔴，代表問題不在 code 而在 spec 或設計 → **上升，不要繼續審**。

收斂判準：**🔴 歸零，且這一輪沒有新的結構性發現。**

---

## 五、從中間切入

整條鏈不必每次從頭走。常見的切入點：

| 情境 | 從哪支開始 |
|---|---|
| 想法還沒成形 | `/shan-grill` |
| 已經想清楚，直接要規格 | `/shan-to-spec` |
| **spec 是別的工具或別人寫的** | `/shan-spec-qa`（先驗過再開工） |
| spec 已定稿，要排開發 | `/shan-plan` |
| 只有一兩個小任務，不值得排計畫 | 直接 `/shan-implement` |
| 手上是一段沒有 spec 的改動 | `/shan-code-review`（意圖軸自動走無 spec 模式） |

前面沒跑過的階段，後面的 skill 會自己降級處理——例如 `shan-implement` 找不到 session 地圖就直接讀 spec，`shan-code-review` 找不到 spec 就改判改動本身的正確性。

---

## 六、幾個刻意的取捨

**spec 要你手動搬。** 換來的是「skill 永遠不寫 spec 目錄」這條不需要判斷的硬規則——護欄一旦變成要判斷的，就是它最容易失守的形式。

**一棒一個視窗。** 操作成本換 context 乾淨。自動審查已經吃掉一半的視窗開銷，剩下的手動審查只有重點棒次才需要。

**審查不自動修 🟡。** 只有 🔴 直接修。架構債型的 🟡 會明確推薦正解並講複利成本，但決定權留給你——因為那類改動的範圍判斷需要你對專案節奏的認識。
