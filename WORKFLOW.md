# 工作流走查

一遍完整的使用歷程，用一個具體功能走過七個階段：**「結帳時套用優惠券」**。

---

## 一、七個階段

| 階段 | 吃什麼 | 吐什麼 | 視窗 |
|---|---|---|---|
| `/shan-skills:shan-setup` | 專案本身 | `.shan/config.md` + `.shan/guard.yaml` | 任何視窗，一個 repo 跑一次 |
| `/shan-skills:shan-grill` | 你的想法 | `<草稿區>/<slug>/grill.md` | 視窗 A |
| `/shan-skills:shan-to-spec` | `grill.md`（+ `findings.md`） | `<草稿區>/<slug>/spec-draft/` | 通常接續 A |
| `/shan-skills:shan-spec-qa` | spec 草稿 | 修訂後的 spec + `qa-report.md`（+ `issues/`） | 任何視窗，它自己 spawn 冷 subagent |
| **你手動把 spec 搬進正式目錄** | | | 人工核可閘門；hook 擋住 skill 寫入 |
| `/shan-skills:shan-plan` | spec + `qa-report.md` + `findings.md` + 未 resolved 的票 | `session-map.md` + 開場 prompt | 新視窗 |
| `/shan-skills:shan-implement` | 一棒 | 綠燈 + commit + 自動審查 + 交接檔 | **每棒一個全新視窗** |
| `/shan-skills:shan-code-review` | commit 後的 diff | 雙軸報告 + 「呼叫端待辦」 | **自動 fork，不看呼叫端對話** |

**視窗紀律只剩兩處是硬的**：實作每棒各自新視窗、第 2 輪審查另開視窗。審查者的乾淨 context 在 v2 已經是機制——`shan-code-review` 宣告 `context: fork`，從哪裡呼叫都一樣冷；`shan-spec-qa` 的語意審查自己 spawn 冷 subagent。

其餘都可以接續同一個視窗。`shan-to-spec` 讀的是 `grill.md` 檔案而不是對話，所以理論上可以新開；接續同一個視窗只是省事。

草稿區裡每個檔案誰產、誰讀、追加還是覆寫，見 [docs/scratch-contract.md](./docs/scratch-contract.md)。

---

## 二、走一遍：結帳套用優惠券

### 階段 0｜`shan-setup`

只跑一次。它探索專案、把發現攤開、逐節問你，最後寫出兩份檔：

- `.shan/config.md`——後面每一支 skill 的專案知識來源。v2 會特別問三件 v1 沒問的：哪些是**每棒必讀**的流程規範、審查修正的 commit 政策、哪些路徑要保護
- `.shan/guard.yaml`——hook 讀的護欄。寫完它會對受保護路徑試一次無害的 Edit，被拒代表 harness 在運作

順帶掃 `.claude/skills/` 有沒有同職責、會自動觸發的舊 skill，讓你決定停用、搬走或刪除。

### 階段 1｜`shan-grill`

```
/shan-skills:shan-grill 我想讓使用者結帳時可以套用優惠券
```

它先讀 config，再依 config B 節讀專案文件，然後開場：

> slug 用 `checkout-coupon`。這次審訊要問到「優惠券的作用單位、疊加規則、折扣在整條金額鏈的位置」都定案為止。

**Round 1** —— 前沿是「前置條件都已定案」的那一圈問題：

```
❓ Q1 — 優惠券的作用單位
整張訂單打折，還是單一品項？兩者在退款與部分取消時的攤提方式完全不同。
➡️ 建議：整張訂單。品項級折扣會逼你在退款時處理攤提，那是另一個量級的複雜度。

❓ Q2 — 可以疊加多張嗎
➡️ 建議：不可疊加，一張訂單一張券。疊加會帶出順序、上限、互斥規則三組新問題。

❓ Q3 — 折扣算在稅前還是稅後
➡️ 建議：稅前。稅金應該對實付金額課徵。
```

注意**「疊加時的計算順序」沒有出現在第一輪**——它依賴 Q2。這就是決策前沿。

同時它會**自己去查**（不會拿來問你）：現在的金額計算走哪條路徑、有沒有既有的折扣概念、資料表長怎樣。查到的事實寫進 `grill.md` 的「已查核的現況事實」表。

**Round 2** —— Q1–Q3 定案後解鎖的問題：

```
❓ Q4 — 折扣計算放在哪一層
(a) 在 CheckoutService 結帳流程裡直接算（最小 patch）
(b) 做成定價規則的一等公民，併進既有的金額計算鏈（架構一致的正解）
➡️ 建議 (b)。(a) 確實比較快，但金額計算鏈已經是「訂單最終金額」的單一收斂點，
   另開一條平行的折扣邏輯會讓金額來源分裂成兩處。而且下一個促銷需求
   一定會再遇到同一個決定——現在分裂，債會複利。
```

Q4 就是「兩案併陳 + 推薦立場」在運作——**它不會默默端出 (a) 然後把 (b) 埋進「未來可以考慮」**。

**Round 3** 收斂，前沿清空。產出 `grill.md`：D1–D6 已敲定、待決為空、明確排除三項。

### 階段 2｜`shan-to-spec`

先檢查 `grill.md` 的「待決」是不是空的——不是就停下來叫你回去 grill。**它不會自己替未決事項做決定。**

然後是整支唯一會停下來的地方：**議定 seam**。

> ① 金額計算鏈的入口（既有 seam，最高點）② 結帳 endpoint（既有 seam）。不新增 seam。
> 依 config E 節：① 碰 DB → 整合測試基底；② → controller 切片測試。這樣對嗎？

確認後它一次寫完，交付時列出產出規模、seam 摘要、以及**它自行補的假設**——那一段要你複核。決策編號 **D1–D6 直接接續 `grill.md`**。

### 階段 3｜`shan-spec-qa`

先分級：動到熱路徑 ✓、含 migration ✓、改動既有流程 ✓ → 閘門 4 跑一輪。

```
閘門 1 格式    對照專案的格式契約，標題逐字比對                    ✅
閘門 2 識別字  backtick 包的識別字逐一實際查證
              自稱既有卻找不到的 = hallucination 的形狀            ✅
閘門 3 機械    任務回指集合 A vs 需求編號集合 B
              B − A = {5.2} ← Req 5.2 沒被任何任務涵蓋            ⚠️
閘門 4 語意    Agent 工具 spawn 冷 subagent，prompt 只有 spec + codebase 路徑
```

閘門 4 的典型收穫——**新舊路徑交界處**：

> 🔴 spec 只描述了「結帳時套用折扣」，但**退款**也會算訂單金額。退款要按折扣後金額退，還是按原價退？spec 完全沒提，而 `RefundService` 目前直接讀訂單原始金額。

這種東西作者自己重讀十遍也看不到。

修訂之後**重跑閘門 2、3**。結果寫進 `qa-report.md`；「退款按哪個金額」這種要你拍板的，開一張 `issues/01-refund-amount-basis.md`，狀態 `ready-for-human`。

### 階段 4｜你手動搬 spec

`spec-draft/` 的檔案由**你**搬進正式的 spec 目錄。skill 不會代做——那一步是人工核可閘門，而且 `guard.yaml` 讓 hook 擋住任何 skill 對正式目錄的寫入。

### 階段 5｜`shan-plan`（新視窗）

讀 spec、`qa-report.md`、`findings.md`、未 resolved 的票。四維評估後切三棒：

```
S1  migration + Coupon 資料模型 + 資料存取     不可逆變更 → 高風險 → 獨立一棒
S2  折扣併入金額計算鏈 + 測試                  依賴 S1
S3  結帳 API 帶入優惠券 + 稽核                 依賴 S1；與 S2 無依賴 → 同屬前沿
```

然後問**三個具體問題**（粒度、阻塞關係、該合併或再拆的），不是「這樣 OK 嗎」。

產出 `session-map.md`，尾端有 `## 修訂記錄`，以及可複製的開場 prompt：

```
/shan-skills:shan-implement 先讀 <草稿區>/checkout-coupon/session-map.md 的「共通背景」「S1」細節與「修訂記錄」，再讀同目錄的 findings.md 與 issues/ 中未 resolved 的票，然後依該棒範圍（任務 1）實作。做完跑該棒檢查點，綠燈後勾選對應任務，commit 前先讓我確認。
```

### 階段 6｜`shan-implement`（每棒全新視窗）

貼開場 prompt，它會：

```
讀 config（B 節必讀全讀）+ spec + 地圖的 S1 段 + findings.md + 未 resolved 的票
 → 跟你確認任務範圍
 → seam 已在設計文件議定 → 照用，不重新協商
 → 開分支（到這一步才開；hook 會擋預設分支上的 commit）
 → 紅綠迴圈：一個測試 → 一份最小實作 → 重複
    ⚠️ 明文禁止「先寫完所有測試再寫實作」
 → 跑 config D 節的完整驗證（外部服務不可用就停下來問，不繞過）
 → 勾選任務
 → 依 config G 節處理 commit
 → 用 Skill 工具呼叫 shan-skills:shan-code-review checkout-coupon S1 <起點>
    （它是 fork：乾淨 context、兩軸各一 agent 平行、只回報）
 → 照報告的「呼叫端待辦」：原文轉述 → 等你裁決 → 修 → 依 G 節 commit
 → 寫 review-S1.md、findings.md、issues/（若有）
 → 報告 S1 結束，不會自己接著做 S2
```

實作中發現 spec 矛盾（例如 Req 4.10 與設計決策 D5 對同一件事說法不同）：**停止寫程式、開票、告知你**。對話會消失，票不會。

### 階段 7｜`shan-code-review`

自動那輪抓明顯的東西。回報**並排、不跨軸重排**：

```
## 意圖軸
Req 3.1 ✅   Req 3.2 🔴 「券在下單與付款之間過期」的邊界沒有測試（spec 原文：…）

## 規範軸
🔴 無
🟡［架構債型 — 附推薦立場］
   折扣金額只存在計算結果裡，沒有任何 INFO log。金額對不上時無從追查是哪張券。
   推薦補一筆里程碑 —— 這個模式會被 S3 的結帳端複製。

## 有異議
規範軸判某段 Javadoc 為捏造事實、意圖軸判無妨——主審查證後採規範軸，理由：…

## 呼叫端待辦
1. 原文完整轉述  2. 🔴 經確認後修  3. 依 G 節 commit  4. 寫 review-S1.md  5. findings / issues / 地圖修訂  6. 第 2 輪另開視窗
```

**兩軸分開報，不挑總冠軍**——完全符合規範但做錯東西、完全照 spec 做但違反慣例，這兩種要能各自被看見。**審查者自己不動手**，所以自動輪（呼叫端是作者）與手動輪（呼叫端是乾淨視窗）拿到的是同一份指令。

需要第 2 輪就另開視窗：

```
/shan-skills:shan-code-review checkout-coupon S1 <第 1 輪的修正 commit>
```

---

## 三、跑完留下什麼

```
<spec 目錄>/checkout-coupon/     ← 你手動搬進來的，進版控；hook 保護
   需求 / 設計 / 任務三份文件

<草稿區>/checkout-coupon/         ← gitignore，過程產物（格式見草稿區契約）
   grill.md          決策的來源，含當時查到的事實
   spec-draft/       草稿（已搬走，留著當歷史）
   qa-report.md      四道閘門的結果，每輪一節
   session-map.md    開發計畫 + 修訂記錄
   findings.md       跨棒事實：S1 查到 DB 會正規化 JSON 鍵序 → S2 的斷言不能用字串相等
   issues/           01-refund-amount-basis.md（resolved）、02-…
   review-S1.md      審查記錄，每輪一節

.shan/config.md、.shan/guard.yaml   ← 進版控

git log    3 個實作 commit + 若干修正 commit，各帶回指 footer
測試        帶驗收條件回指前綴，可對回需求
```

**可追溯性從頭貫到尾**：

```
grill.md D4 → 設計文件 D4 → Req 4.1 → 測試的 Req 4.1 前綴 → commit footer
                                    ↘ issues/03 記錄「當初為什麼改成這樣」
```

---

## 四、審查回饋怎麼接

### 誰動手

**審查者從不動手，呼叫端動手。** v1 的論證是「在審查視窗改，不要貼回實作視窗」，理由有三：轉述必失真、貼回去會觸發辯護、審查者當下最清楚該改什麼。v2 把審查者 fork 出去之後，這三個理由仍然成立，但落點變了：

- **手動輪**：你另開的那個視窗就是呼叫端，它是乾淨的，直接依你的裁決修——效果同 v1 的「在審查視窗改」
- **自動輪**：呼叫端是作者。它只能**原文完整轉述**、等你指定要修哪幾條。摘要權在你不在它

**兩個例外要回實作視窗問**：你**不同意**審查者的判斷時（目的是取得裁決依據，不是叫它改）；修正牽涉**設計決策**時（那應該上升到你，甚至回 spec——開票）。

### 多輪怎麼接

**每一輪都開全新視窗**，但帶一份累積的 `review-S<X>.md`。第 2 輪的開場 prompt 帶 slug、棒次、上一輪的修正 commit，skill 自己讀記錄檔判定輪次。這樣同時解掉三件事：**視窗仍然乾淨**、**不重複報你已裁決的東西**、**上一輪的修正本身進入審查範圍**。

### 怎麼避免越審越肥

多輪審查有個真實的失效模式：**審查者有「找到東西」的壓力**，每一輪降低門檻——第一輪報結構性問題，第二輪報命名，第三輪報「這裡可以抽個 helper」。累積三輪就是三層沒人要求的抽象。

三個煞車寫在 `shan-code-review` 裡：

- **後續輪次只報 🔴。**
- **後續輪次縮小範圍**——審修正 commit 的 diff，不是整棒重掃。
- **硬上限三輪。** 第三輪還在出 🔴，代表問題不在 code 而在 spec 或設計 → **上升，不要繼續審**。

收斂判準：**🔴 歸零，且這一輪沒有新的結構性發現。**

---

## 五、從中間切入

| 情境 | 從哪支開始 |
|---|---|
| 想法還沒成形 | `shan-grill` |
| 已經想清楚，直接要規格 | `shan-to-spec` |
| **spec 是別的工具或別人寫的** | `shan-spec-qa`（先驗過再開工） |
| spec 已定稿，要排開發 | `shan-plan` |
| 只有一兩個小任務，不值得排計畫 | 直接 `shan-implement` |
| 手上是一段沒有 spec 的改動 | `shan-code-review`（意圖軸自動走無 spec 模式） |

前面沒跑過的階段，後面的 skill 會自己降級處理——`shan-implement` 找不到 session 地圖就直接讀 spec，`shan-code-review` 找不到 spec 就改判改動本身的正確性。

---

## 六、幾個刻意的取捨

**spec 要你手動搬。** 換來的是「skill 永遠不寫 spec 目錄」這條不需要判斷的硬規則。v2 更進一步：hook 直接拒絕，連判斷的機會都沒有。

**護欄分兩種。** 「絕不該做」的（寫 spec、amend、force push、預設分支 commit）由 hook 擋；「要判斷」的（未經同意不 commit、不要順手修範圍外的東西）留在 prose。hook 不會忘，但也判斷不了同意。

**一棒一個視窗。** 操作成本換 context 乾淨。自動審查已經吃掉一半的視窗開銷，剩下的手動審查只有重點棒次才需要。

**審查者只回報。** 換來的是自動輪與手動輪行為一致、兩軸不會各自改檔互相衝突。代價是多一次「呼叫端照待辦做」的轉手；為了讓這一步不失真，報告末尾的待辦是固定格式。

**審查不自動修 🟡。** 只有 🔴 經確認後修。架構債型的 🟡 會明確推薦正解並講複利成本，但決定權留給你。

**跨 session 的東西一律落檔。** 對話會消失。事實進 `findings.md`，上升進 `issues/`，地圖的變動進修訂記錄。v1 實跑時這三種都被迫發明出來，v2 只是把它們寫成契約。
